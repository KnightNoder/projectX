.wrapper_with_padding
	#projects.clearfix
		- unless @projects.category.blank?
			- @projects.category.each do |category|
				%a{ href: (url_for [category])}
					.project
						%p.title= category.name
						%p.date= time_ago_in_words(category.created_at)
		- else
			%h2 Add a project
			%p It appears you haven't created any projects yet... Lets fix that. Why don't you go ahead and create a new project.
			%button= link_to "New Category", new_project_path